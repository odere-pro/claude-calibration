# Calibration run artifacts (created by /calibrate)
.claude/calibration/

# Local transient analysis / scratch
tmp/

# Per-run eval snapshots (the tracked time series lives in tests/eval/history.jsonl + baseline.json)
tests/eval/snapshots/

# Transient git worktrees created by agent sessions
.claude/worktrees/

# Personal, machine-local settings
.claude/settings.local.json
CLAUDE.local.md

# OS / editor cruft
.DS_Store
