- Track whether calibration improves a setup over iterations: add the `/calibration-track` flow and its `snapshot.sh` / `compare.sh` engine — a deterministic config-quality snapshot (doctor floor + signature-keyed lint over all nine features) compared vs a baseline anchored to the last PR merged onto `main` and vs the previous iteration, persisted in a local gitignored ledger and independent of `/calibrate`'s circular built-in delta.
