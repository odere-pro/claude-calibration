- Fix two self-audit defects surfaced by dogfooding `/calibrate` against the plugin: `calibrate-claude-md`'s `lint.sh` no longer aborts mid-loop under `set -e`/`pipefail` (it honours the always-exit-0 contract instead of silently masking findings), and the `subagents`/`rules`/`hooks` `enumerate.sh` scripts now skip `CLAUDE.md`/`README.md` so memory/doc files are no longer audited as components (clearing false-positive findings, including an `agents/CLAUDE.md` reported as a broken agent).
