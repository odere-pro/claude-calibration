- Add: a new user-callable flow `/claude-calibration:calibration-audit-parallel` that runs the read-only calibration audit as a Claude `Workflow`. The `SKILL.md` launcher resolves the plugin paths and starts the shipped `skills/calibration-audit-parallel/workflow.mjs`, which fans the nine `calibration-feature-evaluator` workers out via a deterministic `parallel()` barrier (instead of LLM-batching them inside `calibration-evaluator`), then reuses `calibration-evaluator` (Pass-1 steps 4–8) for the merge + interactions + intent-flow synthesis — same reports as `/claude-calibration:calibration-audit`, written under `.claude/calibration/<ts>/`.
