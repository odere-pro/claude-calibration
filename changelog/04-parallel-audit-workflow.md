- Add: a new user-callable flow `/claude-calibration:calibration-audit-parallel` that parallelizes the read-only calibration audit with a headless `claude -p` fan-out. The `SKILL.md` launcher runs the shipped `skills/calibration-audit-parallel/scripts/run-parallel-audit.sh`, which launches one `claude -p` process per feature (nine in parallel, each acting as `calibration-feature-evaluator`), then runs a single synthesis pass (`calibration-evaluator`, Pass-1 steps 4–8) to merge the drafts into the baseline reports under `.claude/calibration/<ts>/`. Same reports and read-only contract as `/claude-calibration:calibration-audit`; the script is also runnable standalone in a terminal or CI.
