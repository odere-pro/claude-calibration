- Add: ship a `Workflow`-tool script `workflows/calibration-audit-parallel.mjs` (run via the `Workflow` tool / `/workflows`, not a skill) that parallelizes the read-only calibration audit — it fans the nine `calibration-feature-evaluator` workers out via a deterministic `parallel()` barrier, then reuses `calibration-evaluator` (Pass-1 steps 4–8) for the merge + interactions + intent-flow synthesis. Since plugins have no native workflow component, a new `SessionStart` hook (`hooks/install-workflows.sh`) installs the bundled workflow into the project's `.claude/workflows/` registry on session start — idempotent, and it never clobbers local edits. Same reports as `/claude-calibration:calibration-audit`, under `.claude/calibration/<ts>/`.
