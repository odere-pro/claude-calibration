- Add: ship a `Workflow`-tool script `.claude/workflows/calibration-audit-parallel.mjs` (run via the `Workflow` tool / `/workflows`, not a skill) that parallelizes the read-only calibration audit — it fans the nine `calibration-feature-evaluator` workers out via a deterministic `parallel()` barrier, then reuses `calibration-evaluator` (Pass-1 steps 4–8) for the merge + interactions + intent-flow synthesis. Same reports as `/claude-calibration:calibration-audit`, written under `.claude/calibration/<ts>/`; cloned into the plugin cache with the rest of the repo.
