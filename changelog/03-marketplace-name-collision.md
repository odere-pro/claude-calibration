- Fix: make `.claude-plugin/marketplace.json` match the working `odere-pro/claude-oop-excellence` shape so `/plugin install claude-calibration@odere-pro` resolves. The manifest was missing the top-level `description` and used `owner.name: "odere"` (the marketplace `name` is `"odere-pro"`); added the top-level `description` and aligned `owner.name` to `"odere-pro"`. Install command is unchanged: `/plugin install claude-calibration@odere-pro`.
