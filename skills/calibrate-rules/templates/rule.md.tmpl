---
paths:
  - "<glob 1, e.g. src/api/**/*.ts>"
  - "<glob 2, optional>"
# Omit `paths:` ONLY when the rule is genuinely always-relevant (rare).
# Brace expansion supported: src/**/*.{ts,tsx}
---

# <Topic title>

<one-line summary of when this rule applies and what it's about.>

- <concrete, verifiable rule>
- <concrete, verifiable rule>
- <concrete, verifiable rule>

<!--
  Keep under ~200 lines. If it grows beyond that, split: one topic per file.
  If this is actually a multi-step workflow you'd trigger with /name, write a skill instead.
-->
