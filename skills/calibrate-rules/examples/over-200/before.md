# Testing — TypeScript

(Imagine ~250 lines covering unit tests, integration tests, E2E tests, mocking strategy,
test naming, snapshot tests, fixtures, coverage targets, CI configuration, and flake handling,
all in one file. No `paths:` frontmatter.)

- Always write tests first (TDD)
- Aim for 80% coverage
- Use Vitest for unit tests
- Use Playwright for E2E
- … (lots more)
- … (and more)
- … (and more — well past 200 effective lines)
